version: '3.8'

services:
  mcp-server:
    build: .
    command: fastmcp run vacation_flight_server.py --transport sse --host 0.0.0.0 --port 8080
    ports:
      - "8080:8080"

  adk-agent:
    build: .
    command: adk web --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      - MCP_HOST=http://mcp-server:8080
    depends_on:
      - mcp-server
